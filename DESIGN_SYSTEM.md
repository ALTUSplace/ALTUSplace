# نظام تصميم ALTUSplace Morocco

## الهوية البصرية

تعتمد المنصة على هوية نظيفة وعصرية لسوق إلكتروني موثوق: خلفيات بيضاء نقية، تدرّج رمادي بارد للأسطح الثانوية، ولون إجراء أساسي واحد هو **Deep Trust Blue** (`#1A56DB`) للثقة والموثوقية. النص بلون فحمي بارد (`#1C1C1E`)، ويُحتفظ بلمسة **Amber** دافئة (`#B7791F`) للتقييمات واللمحات المميزة فقط. تُستخدم ألوان الحالة الأخضر والأحمر للتحقق والتنبيه حصراً. الوضع الداكن (Dark Mode) مدعوم بالكامل بدرجات رمادية باردة.

## الرموز العامة

| المجال | الرمز المعتمد | الاستخدام |
|---|---|---|
| الألوان | `--accent-primary` و`--bg-*` و`--ink-*` و`--ramp-*` | كل الصفحات والمكونات |
| المسافات | `--space-section` وTailwind spacing | فصل الأقسام والحاويات |
| الزوايا | `--radius-xs` إلى `--radius-3xl` | البطاقات، الأزرار، النوافذ |
| الظلال | `--shadow-xs` … `--shadow-xl` و`--shadow-card` و`--shadow-button` | التسلسل البصري دون حدود ثقيلة |
| الحركة | `--dur-fast` و`--dur-standard` و`--ease-smooth` | تفاعلات قصيرة قابلة للمقاطعة |
| اللمس | `--tap-target` و`.touch-target` | مساحة لا تقل عن 44px للعناصر التفاعلية |

### لوحة الألوان الأساسية

- **الإجراء الأساسي:** `#1A56DB` (Hover `#1546B4`، Soft `rgba(26, 86, 219, 0.10)`).
- **الحبر:** `#1C1C1E` / `#6E6E73` / `#8A8A90`.
- **الأسطح:** `#FFFFFF` (البطاقات والصفحة) و`#F7F7F8` (السطح الباهت).
- **اللمسة الدافئة:** `#B7791F`.
- **الحالات:** النجاح `#059669`، الخطأ `#DC2626`، التنبيه `#B45309`.
- **الوضع الداكن:** `#0F1115` / `#16181D` / `#1E2127` مع إجراء أساسي `#4F86F7`.

## المكونات

تستخدم الأزرار ومكونات Shadcn/UI المتاحة في المشروع، مع أصناف `b2-primary-action` للإجراء الأساسي، و`b2-card` للبطاقات، و`b2-focus-surface` و`.focus-ring` لحالة التركيز. البطاقات والأزرار والحقول تستعمل زوايا مستديرة ناعمة (`--radius-lg` إلى `--radius-2xl`). حالات التحميل والفراغ والخطأ يجب أن تظهر داخل سياق الصفحة، وأن توفر رسائل مفهومة وإجراء إعادة المحاولة حيث يكون ذلك ممكناً.

## RTL وLTR

اللغة العربية تعرض `dir="rtl"` و`lang="ar"`، والفرنسية تعرض `dir="ltr"` و`lang="fr"`. يجب استعمال `margin-inline` و`padding-inline` و`inset-inline` بدل الاتجاهات الثابتة، وعدم افتراض أن رمز السهم أو ترتيب الحقول ثابت في اللغتين.

## الاستجابة وإمكانية الوصول

التصميم يبدأ من الهاتف، مع أهداف لمس لا تقل عن 44px، حالات `:focus-visible` واضحة، نصوص بديلة للصور، وتعطيل الحركة غير الضرورية عند `prefers-reduced-motion: reduce`. الصور الكبيرة تستخدم التحميل الكسول وأبعاداً محددة، وتعرض الصفحات حالات التحميل والخطأ والنتائج الفارغة بدلاً من ترك مناطق بيضاء غير مفسرة.

## قيود الإنتاج

CMI والبريد الإلكتروني وWhatsApp محاكاة أو قنوات اختيارية إلى أن تتم إضافة بيانات الاعتماد والتكامل التجاري الفعلي. لا يجوز عرض تقييمات أو شهادات أو أرقام أداء مصطنعة؛ عند غياب بيانات موثقة تعرض الواجهة عبارة عدم وجود مراجعات موثقة بعد.
